const config = {
  server: {
    host: 'localhost',
    port: 8080,
  },
  analyst: {
    host: 'localhost',
    port: 8081,
  },
  mongodb: {
    host: 'mongodb://localhost:27017/shares',
  },
};

module.exports = config;
